bucket           = "${BACKEND}"
region           = "${AWS_REGION}"
service          = "${SERVICE}"
hub_account_id   = "${HUB_ACCOUNT_ID}"
spoke_account_id = "${SPOKE_ACCOUNT_ID}"
ado              = ${ADO}
